[package]
name = "qcm-papier-desktop"
version = "0.1.0"
description = "Application de bureau QCM-Papier (enveloppe Tauri de l'interface web Python)"
edition = "2021"
license = "GPL-3.0-or-later"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
